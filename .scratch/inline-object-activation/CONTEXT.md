# CONTEXT — inline-object-activation

**Inline-object activation** = ordinary link / link-reference / wiki / image / raw HTML share **one** activation + edit contract on `office-view-markdown.markdownViewer`. Shared **document-target** (kind, display vs destination, activation, editable fields, host-open, focus anchor). Edits → **ADR 0009** one intent / one undo-dirty. Focus return → **ADR 0010** document position surviving save/remove/re-render. Wiki host-open (`wiki:`) uses the same target object—not a parallel click grammar. Raw HTML may stay a source popover; save is still one semantic tx preserving enclosing position. **ui 观感 provisional** (click/modifier/refocus) awaiting Studio notes — suggest theme-affine affordances, deterministic activation, focus return after save. Surface under test: `markdownViewer` only. Lead ADR: **0012** (architect `4dfe340`). Done upstream: semantic tx (0009), caret continuity (0010), semantic-find (0011). Out: FindBar reopen, pain #5 async generation guards, Welcome / LLM / second preview.
