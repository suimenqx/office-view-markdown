# CONTEXT — unintended-write-fidelity

**Unintended write-back fidelity** = no Markdown byte rewrite without user intent on `office-view-markdown.markdownViewer`, **no new surface**. Lute `Md→DOM→Md` table normalize (`|a|b|`→`| a | b |`) plus passive `afterRender`→`getMarkdown`→host save currently dirties untouched tables; host only skips when equal after `\r` strip. (1) No-intent write gate / source fingerprint — skip save when no authored intent / byte-equivalent. (2) Table format preserve on Md↔DOM for untouched tables. (3) Dirty-region or span-preserving sync so editing one cell does not rewrite all tables. **ui 观感 provisional** = no dirty flash when user did not edit; table reformat without intent is fail. Prefer on-disk formatting over pretty serialization. Lead ADR: **0015** (architect `f591eea`). Forge order **01 → 02 → 03**, estimate 2.5–4.5d. Upstream closed: paste/table (0007), semantic tx (0009), residual hardening (0014). Do **not** reopen those as greenfield; do **not** ship Option 2 / Welcome / LLM / second preview.
