# CONTEXT — semantic-find

**Document-semantic Find** = Find / Find Next / Replace / Replace All over the canonical authored document stream—not separate DOM text walks + CodeMirror scans that disagree on count and undo. **Shared source ranges** for prose, inline markup, and embedded code/math; crossing emphasis/link boundaries must not drop a hit. **Mounting** a block to show a match must not change the result set. Replace Current / Replace All → **ADR 0009** one intent / one undo unit; restore via **ADR 0010** document position. **No new Find skin** — reuse existing FindBar chrome; theme-affine match highlight + honest count (ui Studio notes pending). Surface under test: `office-view-markdown.markdownViewer` only. Lead ADR: **0011**. Done upstream: semantic tx (0009), caret continuity (0010).
