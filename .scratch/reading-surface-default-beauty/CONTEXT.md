# CONTEXT — reading-surface-default-beauty

**Reading Surface default beauty** = first open of `office-view-markdown.markdownViewer` looks finished, **no new surface**. (1) Toolbar retention — primary ≤14 visible; merge theme into one entry; drop upload from primary (settings/more still reach them). (2) Default type + measure — Editor Font Size **16px** via `office-view-markdown.editorFontSize` (keep `0` = follow VS Code); line-height **1.75**; measure **`min(100%, 52rem)`**; VS Code font family only. (3) Auto theme + `--vscode-*` chrome for table/outline/toolbar/blockquote across Light/Dark/HC; appearance changes must not dirty / must not trigger ADR 0015 rewrite. **ui 观感 locked** = those three Studio items. Lead ADR: **0016** (architect `6af04ff`). Forge order **01 → 02 → 03**, estimate 2–4d. Upstream closed: unintended-write-fidelity (0015). Do **not** reopen 0015; do **not** ship Welcome / second preview / remote fonts / line-height Settings exposure / more-menu polish (backlog).
