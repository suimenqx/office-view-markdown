# CONTEXT — async-render-generation

**Async-render generation** = PlantUML / Mermaid / image async results commit on `office-view-markdown.markdownViewer` only when `(stable block identity, source revision, renderer/theme config)` is still current and the host element is connected. Stale in-flight writes after edit / undo / delete / replace / theme refresh are discarded. **Retry** re-reads current source (not a closed-over snapshot). Loading / ready / error are mutually exclusive and disposable. Must **not** invent undo (ADR 0009) and must **not** steal focus anchors (ADR 0010). **AES unchanged (ADR 0003)** — Open Settings / Retry family; no default public PlantUML server, no silent probing (ADRs 0001–0002). **ui 观感 provisional** (no flash of stale diagram; no flicker between generations) awaiting Studio notes. Surface under test: `markdownViewer` only. Lead ADR: **0013** (architect `4ad17be`). Done upstream: semantic tx (0009), caret continuity (0010), semantic-find (0011), inline-object activation (0012). Out: FindBar skin, Welcome / LLM / second preview, reopen 0012/0011/0009/0008, AES redesign.
