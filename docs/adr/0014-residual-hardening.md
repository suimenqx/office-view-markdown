# Residual hardening closes trust gaps without new surfaces

After ADR 0007–0013, the next slice hardens three product-visible residuals only—no new Reading Surface features: (1) Find input must not steal focus into CodeMirror or mutate document body while typing the query; (2) inline-object plain-click → edit popover → save → refocus must complete on `markdownViewer` under the ADR 0012 contract (not unit-only); (3) prose edits that still path through debounced `afterRender` must group as one ADR 0009 intent (one undo unit / one dirty pulse). Harness/Dark/HC debt stays secondary unless a Light bug is proven. Acceptance: existing SMOKE families extended, still `markdownViewer` only. Out of scope: Option 2 density polish, Welcome/second preview/LLM, reopening closed waves as greenfield.
